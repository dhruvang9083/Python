divisor = eval(input('Please enter number to divisor: '))
dividend = eval(input('Please enter number to dividend: '))
if divisor != 0:
    quotient = dividend/divisor
    print("ans", quotient)
    print('Program finished')
